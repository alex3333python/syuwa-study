abstract final class AppFonts {
  // iPad Safari system font — no bundled TTF download on first load.
  static const String interface = 'Hiragino Sans';
  static const String display = 'Hiragino Sans';
}
